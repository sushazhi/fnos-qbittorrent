#!/bin/bash
# cmd/uninstall_callback - qBittorrent卸载回调脚本

set -e

APP_NAME="qbittorrent"
ERROR_LOG="${TRIM_TEMP_LOGFILE:-/tmp/${APP_NAME}_error.log}"

echo "Cleaning up qBittorrent..."

# 清理临时文件和缓存
rm -f "${TRIM_PKGTMP}/qbittorrent.pid"

echo "qBittorrent cleanup completed"
exit 0
