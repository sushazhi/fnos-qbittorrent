#!/bin/bash
# cmd/main - qBittorrent应用生命周期管理脚本

APP_NAME="qbittorrent"
BIN_DIR="${TRIM_APPDEST}/bin"
DATA_DIR="${TRIM_PKGVAR}"
CONFIG_FILE="${DATA_DIR}/qBittorrent/config/qBittorrent.conf"
PID_FILE="${TRIM_PKGTMP}/${APP_NAME}.pid"
LOG_FILE="${TRIM_PKGVAR}/${APP_NAME}.log"

# 日志轮转配置
LOG_MAX_SIZE=1048576  # 1MB
LOG_MAX_FILES=3        # 保留3个旧日志文件

# 端口配置
WEBUI_PORT="${wizard_app_port:-${TRIM_SERVICE_PORT:-8080}}"

# 日志轮转函数
rotate_logs() {
    if [ -f "$LOG_FILE" ]; then
        LOG_SIZE=$(stat -c%s "$LOG_FILE" 2>/dev/null || stat -f%z "$LOG_FILE" 2>/dev/null || echo 0)
        if [ "$LOG_SIZE" -gt "$LOG_MAX_SIZE" ]; then
            # 删除最旧的日志
            if [ -f "${LOG_FILE}.${LOG_MAX_FILES}" ]; then
                rm -f "${LOG_FILE}.${LOG_MAX_FILES}"
            fi
            # 轮转现有日志文件
            for i in $(seq $((LOG_MAX_FILES - 1)) -1 1); do
                if [ -f "${LOG_FILE}.${i}" ]; then
                    mv "${LOG_FILE}.${i}" "${LOG_FILE}.$((i + 1))"
                fi
            done
            # 移动当前日志
            mv "$LOG_FILE" "${LOG_FILE}.1"
            echo "$(date '+%Y-%m-%d %H:%M:%S') - Log rotated" > "$LOG_FILE"
        fi
    fi
}

case "$1" in
start)
    # 检查是否已运行（通过PID文件）
    if [ -f "$PID_FILE" ]; then
        PID=$(cat "$PID_FILE")
        if ps -p "$PID" > /dev/null 2>&1; then
            echo "${APP_NAME} is already running (PID: $PID)"
            exit 0
        fi
        rm -f "$PID_FILE"
    fi

    # 检查系统中是否存在其他 qbittorrent-nox 进程（可能是飞牛下载等）
    EXISTING_PID=$(pgrep -f "qbittorrent-nox" 2>/dev/null | head -1)
    if [ -n "$EXISTING_PID" ]; then
        # 检查是否是本应用的进程
        EXISTING_CMD=$(ps -p "$EXISTING_PID" -o args= 2>/dev/null)
        if echo "$EXISTING_CMD" | grep -q "profile.*${DATA_DIR}"; then
            # 是本应用的进程，但PID文件丢失，尝试恢复
            echo $EXISTING_PID > "$PID_FILE"
            echo "${APP_NAME} is already running (PID: $EXISTING_PID, recovered)"
            exit 0
        fi
        # 其他应用的进程（如飞牛下载 trim-qbittorrent），仅记录日志，不阻止启动
        echo "提示：检测到其他 qBittorrent 进程正在运行 (PID: $EXISTING_PID)，继续启动本应用"
    fi

    # 创建必要的目录
    mkdir -p "$(dirname "$PID_FILE")" "${DATA_DIR}/qBittorrent/config"

    # 日志轮转
    rotate_logs

    # 启动前端口预检测
    if command -v ss >/dev/null 2>&1; then
        PORT_CHECK=$(ss -tln 2>/dev/null | grep -c ":${WEBUI_PORT} " || true)
    elif command -v netstat >/dev/null 2>&1; then
        PORT_CHECK=$(netstat -tln 2>/dev/null | grep -c ":${WEBUI_PORT} " || true)
    else
        PORT_CHECK=0
    fi
    if [ "$PORT_CHECK" -gt 0 ]; then
        echo "错误：端口 ${WEBUI_PORT} 已被占用，请更换端口后重试" > "$TRIM_TEMP_LOGFILE"
        echo "可在应用设置中修改端口号" >> "$TRIM_TEMP_LOGFILE"
        exit 1
    fi

    # 设置环境变量
    export QT_QPA_PLATFORM=offscreen

    # 检查可执行文件
    if [ ! -f "$BIN_DIR/qbittorrent-nox" ]; then
        echo "错误：未找到可执行文件 qbittorrent-nox" > "$TRIM_TEMP_LOGFILE"
        exit 1
    fi
    if [ ! -x "$BIN_DIR/qbittorrent-nox" ]; then
        chmod +x "$BIN_DIR/qbittorrent-nox"
    fi

    # 只在配置文件不存在时创建默认配置
    if [ ! -f "$CONFIG_FILE" ]; then
        cat > "$CONFIG_FILE" << EOF
[Preferences]
General\Locale=zh_CN
Downloads\SavePath=/vol1/1000/downloads/
Downloads\TempPath=/vol1/1000/temp/
WebUI\Address=*
WebUI\Port=${WEBUI_PORT}
WebUI\LocalHostAuth=true
WebUI\Username=admin
WebUI\Password_PBKDF2=@ByteArray(ARQ77eY1NUZaQsuDHbIMCA==:0WMRkYTUWVT9wVvdDtHAjU9b3b7uB8NR1Gur2hmQCvCDpm39Q+PsJRJPaCU51dEiz+dTzh8qbPsL8WkFljQYFQ==)
WebUI\ClickjackingProtection=false
WebUI\CSRFProtection=false
WebUI\AlternativeUIEnabled=true
WebUI\RootFolder=${TRIM_APPDEST}/ui/vuetorrent
EOF
    fi

    # 启动qBittorrent-nox
    echo "Starting qBittorrent..."
    "$BIN_DIR/qbittorrent-nox" \
        --confirm-legal-notice \
        --profile="${DATA_DIR}" \
        --webui-port="$WEBUI_PORT" \
        >> "$LOG_FILE" 2>&1 &

    PID=$!
    echo $PID > "$PID_FILE"

    sleep 3
    if ps -p "$PID" > /dev/null 2>&1; then
        echo "${APP_NAME} started (PID: $PID)"
        exit 0
    else
        # 检查是否是端口占用问题
        if [ -f "$LOG_FILE" ]; then
            if grep -qi "port.*already\|bind.*failed\|address.*in.*use" "$LOG_FILE" 2>/dev/null; then
                echo "错误：端口 ${WEBUI_PORT} 被其他程序占用，请更换端口后重试" > "$TRIM_TEMP_LOGFILE"
                rm -f "$PID_FILE"
                exit 1
            fi
        fi
        echo "错误：${APP_NAME} 启动失败" > "$TRIM_TEMP_LOGFILE"
        if [ -f "$LOG_FILE" ]; then
            tail -20 "$LOG_FILE" >> "$TRIM_TEMP_LOGFILE"
        fi
        rm -f "$PID_FILE"
        exit 1
    fi
    ;;

stop)
    # 查找本应用的进程
    PID=""
    if [ -f "$PID_FILE" ]; then
        PID=$(cat "$PID_FILE" 2>/dev/null)
    fi
    if [ -z "$PID" ] || ! ps -p "$PID" > /dev/null 2>&1; then
        # 通过 profile 路径精确匹配本应用的进程
        PID=$(pgrep -f "qbittorrent-nox.*--profile.*${DATA_DIR}" 2>/dev/null | head -1)
    fi

    if [ -z "$PID" ]; then
        echo "${APP_NAME} is not running"
        exit 0
    fi

    echo "Stopping ${APP_NAME} (PID: $PID)..."
    kill "$PID" 2>/dev/null || true

    TIMEOUT=10
    while ps -p "$PID" > /dev/null 2>&1 && [ $TIMEOUT -gt 0 ]; do
        sleep 0.5
        TIMEOUT=$((TIMEOUT - 1))
    done

    if ps -p "$PID" > /dev/null 2>&1; then
        kill -9 "$PID" 2>/dev/null || true
    fi

    rm -f "${TRIM_PKGTMP}/${APP_NAME}.pid" 2>/dev/null
    echo "${APP_NAME} stopped"
    exit 0
    ;;

restart)
    "$0" stop
    sleep 2
    "$0" start
    ;;

status)
    RUNNING=false
    if [ -f "$PID_FILE" ]; then
        PID=$(cat "$PID_FILE")
        if ps -p "$PID" > /dev/null 2>&1; then
            RUNNING=true
        fi
    fi
    if [ "$RUNNING" = false ]; then
        # 通过 profile 路径精确匹配本应用的进程
        PID=$(pgrep -f "qbittorrent-nox.*--profile.*${DATA_DIR}" 2>/dev/null | head -1)
        if [ -n "$PID" ]; then
            RUNNING=true
        fi
    fi

    if [ "$RUNNING" = true ]; then
        echo "${APP_NAME} is running (PID: $PID)"
        exit 0
    else
        echo "${APP_NAME} is not running"
        exit 3
    fi
    ;;

*)
    echo "Usage: $0 {start|stop|restart|status}"
    exit 1
    ;;
esac
