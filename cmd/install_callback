#!/bin/bash
# cmd/install_callback - qBittorrent安装后置脚本

set -e

APP_NAME="qbittorrent"
BIN_DIR="${TRIM_APPDEST}/bin"

echo "Running ${APP_NAME} install_callback..."

# 设置可执行权限
if ! chmod +x "$BIN_DIR/qbittorrent-nox" 2>/dev/null; then
    echo "Warning: Failed to set executable permission on qbittorrent-nox"
fi

echo "=================================="
echo "qBittorrent installation complete"
echo "=================================="
echo ""
echo "NOTE: Data directories and configuration"
echo "will be created on first start."
echo ""
echo "Start the application to initialize:"
echo "qBittorrent installed successfully"
exit 0
