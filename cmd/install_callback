#!/bin/bash
# cmd/install_callback - qBittorrent安装后置脚本

APP_NAME="qbittorrent"
BIN_DIR="${TRIM_APPDEST}/bin"
DATA_DIR="${TRIM_PKGVAR}"
CONFIG_DIR="${DATA_DIR}/qBittorrent/config"
CONFIG_FILE="${CONFIG_DIR}/qBittorrent.conf"

echo "Running ${APP_NAME} install_callback..."

is_valid_port() {
    local port="$1"
    [[ "$port" =~ ^[0-9]+$ ]] || return 1
    [ "$port" -ge 1 ] && [ "$port" -le 65535 ]
}

# 设置可执行权限
if ! chmod +x "$BIN_DIR/qbittorrent-nox" 2>/dev/null; then
    echo "Warning: Failed to set executable permission on qbittorrent-nox" > "$TRIM_TEMP_LOGFILE"
fi

# 获取端口配置
WEBUI_PORT="${wizard_app_port:-8080}"
if ! is_valid_port "$WEBUI_PORT"; then
    echo "Warning: invalid port $WEBUI_PORT, fallback to 8080"
    WEBUI_PORT="8080"
fi

# 获取UI类型选择（默认vuetorrent）
UI_TYPE="${wizard_ui_type:-vuetorrent}"

# 获取下载目录配置
SAVE_PATH="${wizard_save_path:-/vol1/@appshare/qbittorrent/downloads/}"
if [ ! -d "$SAVE_PATH" ]; then
    echo "Warning: save path not exists, fallback to /vol1/@appshare/qbittorrent/downloads/"
    SAVE_PATH="/vol1/@appshare/qbittorrent/downloads/"
fi

echo "Configuring UI type: $UI_TYPE"
echo "Port: $WEBUI_PORT"
echo "Save Path: $SAVE_PATH"

# 创建配置目录
mkdir -p "$CONFIG_DIR"

# 确保下载目录存在
mkdir -p "$SAVE_PATH" 2>/dev/null || true

# 根据UI类型设置配置
case "$UI_TYPE" in
    vuetorrent)
        ALT_UI_ENABLED="true"
        ;;
    native)
        ALT_UI_ENABLED="false"
        ;;
    *)
        echo "Unknown UI type: $UI_TYPE, using default vuetorrent"
        ALT_UI_ENABLED="true"
        ;;
esac

# 检查配置文件是否已存在（用户卸载时可能保留了配置）
if [ -f "$CONFIG_FILE" ]; then
    echo "Configuration file exists, updating port/savepath/UI settings while preserving other settings..."

    # 只更新端口、下载目录、UI界面，保留其他用户设置（如用户名密码等）
    # 兼容新旧配置格式：同时更新 Session\DefaultSavePath 和 Downloads\SavePath
    TMP_CONFIG=$(mktemp)
    awk -v port="${WEBUI_PORT}" -v alt_ui="${ALT_UI_ENABLED}" -v save_path="${SAVE_PATH}" '
        BEGIN { port_updated = 0; alt_ui_updated = 0; save_updated = 0; bittorrent_section = 0; }
        {
            if ($0 == "[BitTorrent]") {
                print $0
                print "Session\\DefaultSavePath=" save_path
                bittorrent_section = 1
                save_updated = 1
            }
            else if ($0 == "[Preferences]") {
                print $0
                print "WebUI\\Port=" port
                print "WebUI\\AlternativeUIEnabled=" alt_ui
                port_updated = 1
                alt_ui_updated = 1
            }
            else if (!/^WebUI\\Port=/ && !/^WebUI\\AlternativeUIEnabled=/ && !/^Downloads\\SavePath=/ && !/^Session\\DefaultSavePath=/) {
                print $0
            }
        }
        END {
            if (!port_updated && !alt_ui_updated && !save_updated) {
                print "[BitTorrent]"
                print "Session\\DefaultSavePath=" save_path
                print "[Preferences]"
                print "WebUI\\Port=" port
                print "WebUI\\AlternativeUIEnabled=" alt_ui
            }
        }
    ' "$CONFIG_FILE" > "$TMP_CONFIG"

    mv "$TMP_CONFIG" "$CONFIG_FILE"
    echo "Configuration updated (port, savepath, UI)"
else
    echo "Creating new configuration file..."

    # 尝试从残留配置中保留凭证（卸载后重装场景）
    OLD_USER=""
    OLD_PASS=""
    for old_conf in "${DATA_DIR}/qBittorrent/config/qBittorrent.conf" \
                     "/vol1/@appshare/qbittorrent/.data_backup/qBittorrent/config/qBittorrent.conf"; do
        if [ -f "$old_conf" ]; then
            OLD_USER=$(grep "^WebUI\\\\Username=" "$old_conf" 2>/dev/null | head -1 | sed 's/WebUI\\Username=//')
            OLD_PASS=$(grep "^WebUI\\\\Password_PBKDF2=" "$old_conf" 2>/dev/null | head -1 | sed 's/WebUI\\Password_PBKDF2=//')
            if [ -n "$OLD_USER" ] && [ -n "$OLD_PASS" ]; then
                echo "保留已有凭证（用户名: $OLD_USER）"
                break
            fi
            OLD_USER=""
            OLD_PASS=""
        fi
    done

    # 未找到旧凭证时使用默认值
    if [ -z "$OLD_USER" ]; then
        OLD_USER="admin"
        OLD_PASS='@ByteArray(ARQ77eY1NUZaQsuDHbIMCA==:0WMRkYTUWVT9wVvdDtHAjU9b3b7uB8NR1Gur2hmQCvCDpm39Q+PsJRJPaCU51dEiz+dTzh8qbPsL8WkFljQYFQ==)'
    fi

    # 根据UI类型创建完整默认配置
    case "$UI_TYPE" in
        vuetorrent)
            # VueTorrent: 启用备用UI
            cat > "$CONFIG_FILE" << CFGEOF
[BitTorrent]
Session\DefaultSavePath=${SAVE_PATH}
[Preferences]
General\Locale=zh_CN
WebUI\Address=*
WebUI\Port=${WEBUI_PORT}
WebUI\LocalHostAuth=true
WebUI\Username=${OLD_USER}
WebUI\Password_PBKDF2=${OLD_PASS}
WebUI\ClickjackingProtection=false
WebUI\CSRFProtection=false
WebUI\AlternativeUIEnabled=true
WebUI\RootFolder=${TRIM_APPDEST}/ui/vuetorrent
CFGEOF
            echo "VueTorrent configured (iframe mode)"
            ;;
        native)
            # 原生UI: 禁用备用UI
            cat > "$CONFIG_FILE" << CFGEOF
[BitTorrent]
Session\DefaultSavePath=${SAVE_PATH}
[Preferences]
General\Locale=zh_CN
WebUI\Address=*
WebUI\Port=${WEBUI_PORT}
WebUI\LocalHostAuth=true
WebUI\Username=${OLD_USER}
WebUI\Password_PBKDF2=${OLD_PASS}
WebUI\ClickjackingProtection=false
WebUI\CSRFProtection=false
WebUI\AlternativeUIEnabled=false
WebUI\RootFolder=${TRIM_APPDEST}/ui/vuetorrent
CFGEOF
            echo "Native UI configured (url mode)"
            ;;
        *)
            echo "Unknown UI type: $UI_TYPE"
            ;;
    esac
fi

echo "=================================="
echo "qBittorrent installation complete"
echo "=================================="
echo ""
echo "UI Type: $UI_TYPE"
echo "Port: $WEBUI_PORT"
echo "Save Path: $SAVE_PATH"
echo ""
echo "Start the application to use qBittorrent"
exit 0
